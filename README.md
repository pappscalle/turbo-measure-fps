# dos-template

A simple Turbo Pascal program used as a template.
Target computer: 286-12MHz

Uses external Pascal Units as subtree (`src/external`)

Initialize and download the units, run:
```
./init-units.sh
```

To keep the units up to date, run:
```
./update-units.sh
```
